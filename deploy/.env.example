# ===========================================
# Voice Note Server — Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# ⚠️ NEVER commit .env to Git!

# ---- Required ----

# DashScope API Key (get from https://bailian.console.aliyun.com/)
DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# Client authentication key (X-API-Key header value)
API_AUTH_KEY=your-secret-auth-key

# ---- Optional (defaults shown) ----

# Spring profile: prod (default) or dev
# SPRING_PROFILES_ACTIVE=prod

# CORS allowed origins (comma-separated for multiple domains)
# CORS_ALLOWED_ORIGINS=https://api.suikouji.com

# Trusted reverse proxy IPs for real client IP resolution
# RATE_LIMIT_TRUSTED_PROXIES=127.0.0.1

# Additional JVM options (Dockerfile defaults: G1GC, MaxRAMPercentage=75%, StringDeduplication)
# JAVA_OPTS=

# Host port mapping (container always listens on 8080)
# SERVER_PORT=8080

# Log file output directory (mounted from host, prod profile only)
# LOG_DIR=./logs
